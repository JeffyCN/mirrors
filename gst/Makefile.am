if USE_ROCKCHIPMPP
ROCKCHIPMPP_DIR=rockchipmpp
else
ROCKCHIPMPP_DIR=
endif

if USE_VPUDEC
VPUDEC_DIR=vpudec
else
VPUDEC_DIR=
endif

if USE_RKSINK
RKSINK_DIR=rksink
else
RKSINK_DIR=
endif

SUBDIRS = $(ROCKCHIPMPP_DIR) $(VPUDEC_DIR) $(RKSINK_DIR)

DIST_SUBDIRS = rockchipmpp vpudec rksink
