# NAME		Relative path
#
#HWDEF		HWDEF
package-file	package-file
bootloader	Image/MiniLoaderAll.bin
parameter	Image/parameter.txt
uboot		Image/uboot.img
uboot-dtb	Image/uboot-dtb.img
uboot-ubl	Image/uboot-ubl.img
# env		Image/env.img
kernel-dtb	Image/kernel-dtb.img
kernel		Image/kernel.img
# kernel-bak	Image/kernel-bak.img
# kernel-sm3	Image/kernel-sm3.img
rootfs		Image/rootfs.img
# userdata	Image/userdata.img
custom		Image/custom.img
data		Image/data.img
backup		Image/backup.img
update		Image/update.img
log		Image/log.img
docker		Image/docker.img
# 要写入backup分区的文件就是自身（updateall.img）
# SELF 是关键字，表示升级文件（updateall.img）自身
# 在生成升级文件时，不加入SELF文件的内容，但在头部信息中有记录
# 在解包升级文件时，不解包SELF文件的内容。
#update-script	update-script
#recover-script	recover-script
